# 📋 Аналіз проекту Lionzz Planner

## 🎯 Що це за проект?

**Lionzz Planner** - це веб-додаток для планування завдань з тижневим виглядом, інтегрований з Firebase. Додаток дозволяє користувачам:

- Створювати та керувати завданнями з датами виконання
- Переглядати завдання в тижневому вигляді (7 днів)
- Відстежувати прострочені завдання (штрафні)
- Відмічати завдання як виконані
- Редагувати та видаляти завдання
- Автоматично завантажувати початковий план розвитку AI

## 📁 Структура проекту

```
Lionzz_planner/
├── src/                          # Основний код додатку
│   ├── App.jsx                   # Головний компонент додатку
│   ├── main.jsx                  # Точка входу React додатку
│   ├── index.css                 # Tailwind CSS стилі
│   │
│   ├── components/               # React компоненти
│   │   ├── DayCard.jsx          # Картка дня тижня (відображає завдання дня)
│   │   ├── TaskItem.jsx         # Окреме завдання (чекбокс, назва, видалення)
│   │   └── EditTaskModal.jsx    # Модальне вікно для редагування завдання
│   │
│   ├── hooks/                    # Custom React hooks
│   │   ├── useFirebaseAuth.js   # Хук для автентифікації Firebase
│   │   └── useFirestoreTasks.js # Хук для роботи з завданнями в Firestore
│   │
│   ├── services/                 # Сервіси та конфігурації
│   │   └── firebaseConfig.js    # Конфігурація Firebase (API ключі, projectId)
│   │
│   └── utils/                    # Допоміжні функції
│       └── dateHelpers.js       # Функції для роботи з датами (початок тижня, сьогодні)
│
├── Full/                         # Старий монолітний файл (архів)
│   └── lionzz_planner.jsx       # Повна версія в одному файлі
│
├── index.html                    # HTML шаблон
├── package.json                  # Залежності та скрипти
├── postcss.config.js            # PostCSS конфігурація
├── tailwind.config.js           # Tailwind CSS конфігурація
└── node_modules/                 # Встановлені пакети

```

## 🔧 Технології

- **React 18** - UI фреймворк
- **Vite** - Збірщик та dev сервер
- **Firebase** - Backend (Auth + Firestore)
- **Tailwind CSS** - Стилізація
- **PostCSS** - Обробка CSS

## 🗂️ Детальний опис компонентів

### 1. **App.jsx** - Головний компонент
- Управляє станом додатку
- Інтегрує всі компоненти
- Обробляє CRUD операції з завданнями
- Відображає тижневий вигляд з навігацією
- Показує прострочені завдання

### 2. **useFirebaseAuth.js** - Автентифікація
- Ініціалізує Firebase App та Firestore
- Виконує анонімну автентифікацію
- Повертає `userId`, `db`, `isAuthReady`

### 3. **useFirestoreTasks.js** - Робота з завданнями
- Підписується на зміни в Firestore (real-time)
- CRUD операції: `addTask`, `updateTask`, `deleteTask`
- Функція `loadInitialPlan()` - завантажує початкові тестові дані

### 4. **DayCard.jsx** - Картка дня
- Відображає день тижня (назва, число)
- Показує активні та виконані завдання
- Підсвічує сьогоднішній день

### 5. **TaskItem.jsx** - Елемент завдання
- Чекбокс для відмітки виконання
- Назва завдання (клік для редагування)
- Кнопка видалення (з'являється при наведенні)

### 6. **EditTaskModal.jsx** - Модальне вікно редагування
- Форма для зміни назви та дати завдання
- Кнопки "Зберегти" та "Скасувати"

## 🔥 Структура даних в Firebase

### Колекція завдань
**Шлях:** `/users/{userId}/tasks`

### Структура документа завдання:
```javascript
{
  title: string,              // Назва завдання
  isCompleted: boolean,       // Чи виконано
  dueDate: Timestamp,         // Дата виконання
  createdAt: Timestamp,       // Дата створення
  isDaily: boolean (optional) // Щоденне завдання
}
```

## 📊 Потік даних

1. **Ініціалізація:**
   - `App.jsx` → `useFirebaseAuth()` → ініціалізує Firebase
   - Анонімна автентифікація → отримує `userId`

2. **Завантаження завдань:**
   - `useFirestoreTasks()` → підписується на `/users/{userId}/tasks`
   - Real-time оновлення через `onSnapshot()`

3. **Додавання завдання:**
   - Форма в `App.jsx` → `addTask()` → Firestore `addDoc()`

4. **Оновлення/Видалення:**
   - `updateTask()` / `deleteTask()` → Firestore `updateDoc()` / `deleteDoc()`

## 🚀 Як завантажити тестові дані до Firebase

### Варіант 1: Через UI додатку
1. Запустіть додаток: `npm run dev`
2. Якщо база даних порожня, з'явиться кнопка **"Завантажити початковий план"**
3. Натисніть кнопку - дані автоматично завантажаться

### Варіант 2: Автоматично при першому запуску
Можна модифікувати `useFirestoreTasks.js`, щоб автоматично завантажувати дані:

```javascript
useEffect(() => {
  if (!db || !tasksColPath || isLoading) return;
  
  // Автоматично завантажити, якщо немає завдань
  if (tasks.length === 0 && !isLoading) {
    loadInitialPlan();
  }
}, [db, tasksColPath, tasks.length, isLoading]);
```

### Що завантажується:
Функція `loadInitialPlan()` створює 8 тестових завдань:
- **Сьогодні (2 щоденні + 3 одноразові):**
  - Моніторити gov.uk та Multiverse на нові стажування
  - Розвивати LinkedIn: 15 хв
  - Отримати OpenAI API ключ
  - Оновити CV та LinkedIn
  - Створити репозиторій на GitHub

- **Через 2 дні (2 завдання):**
  - Почати вивчення Prompt Engineering
  - Ознайомитися з Claude API

- **Через 4 дні (2 завдання):**
  - Вивчити PRD
  - Почати перший ШІ-проект

## 🔍 Де знаходяться дані в Firebase Console

1. Відкрийте [Firebase Console](https://console.firebase.google.com/)
2. Виберіть проект `lionzz-planner`
3. Перейдіть в **Firestore Database**
4. Структура: `users` → `{userId}` → `tasks` → `{taskId}`

## ⚙️ Налаштування

### Firebase конфігурація
Файл: `src/services/firebaseConfig.js`
- Містить API ключі та налаштування проекту
- **УВАГА:** Не комітьте цей файл в публічний репозиторій!

### Tailwind CSS
Файл: `tailwind.config.js` (в корені)
- Вказує шляхи до файлів для сканування класів
- Плагін `@tailwindcss/forms` для стилізації форм

## 🐛 Виправлені проблеми

✅ **Tailwind CSS content warning** - виправлено:
- Переміщено `tailwind.config.js` в корінь проекту
- Встановлено плагін `@tailwindcss/forms`
- Шляхи до файлів налаштовані правильно

## 📝 Примітки

- Додаток використовує **анонімну автентифікацію** - кожен користувач отримує унікальний `userId`
- Дані зберігаються окремо для кожного користувача
- Real-time синхронізація - зміни відображаються миттєво
- Тиждень починається з понеділка (налаштовано в `dateHelpers.js`)

