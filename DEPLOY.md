# 🚀 Інструкція з публікації на GitHub

## Крок 1: Створіть репозиторій на GitHub

1. Відкрийте [GitHub](https://github.com) та увійдіть у свій акаунт
2. Натисніть кнопку **"+"** у правому верхньому куті → **"New repository"**
3. Заповніть форму:
   - **Repository name:** `lionzz-planner` (або інша назва)
   - **Description:** "Сучасний веб-додаток для планування завдань з інтеграцією AI"
   - **Visibility:** Public (або Private, якщо хочете приватний репозиторій)
   - **НЕ** ставлять галочку "Initialize this repository with a README" (у нас вже є README)
4. Натисніть **"Create repository"**

## Крок 2: Підготуйте локальний репозиторій

Виконайте ці команди в терміналі (в папці проєкту):

```bash
# Додати всі зміни
git add .

# Створити коміт
git commit -m "feat: очищено код, додано професійний README та покращено структуру проєкту"

# Перейменувати гілку на main (якщо потрібно)
git branch -M main
```

## Крок 3: Підключіть GitHub репозиторій

Після створення репозиторію на GitHub, ви побачите інструкції. Виконайте:

```bash
# Додати remote репозиторій (замініть YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/lionzz-planner.git

# Або якщо використовуєте SSH:
# git remote add origin git@github.com:YOUR_USERNAME/lionzz-planner.git

# Перевірити, що все правильно
git remote -v
```

## Крок 4: Завантажити код на GitHub

```bash
# Завантажити код
git push -u origin main
```

Якщо використовуєте HTTPS і GitHub просить автентифікацію:
- Для Personal Access Token: введіть ваш токен замість пароля
- Або налаштуйте SSH ключі для зручності

## ✅ Готово!

Ваш проєкт тепер на GitHub! Відкрийте репозиторій у браузері та перевірте, що все завантажилося правильно.

---

## 🔄 Для майбутніх оновлень

Коли ви зробите зміни в проєкті:

```bash
# Перевірити зміни
git status

# Додати зміни
git add .

# Створити коміт
git commit -m "опис ваших змін"

# Завантажити на GitHub
git push
```

## 📝 Приклади хороших повідомлень комітів

- `feat: додано нову функцію X`
- `fix: виправлено помилку Y`
- `refactor: покращено структуру коду`
- `docs: оновлено документацію`
- `style: виправлено форматування`

---

**Примітка:** Якщо у вас виникнуть проблеми з автентифікацією, дивіться [GitHub документацію](https://docs.github.com/en/authentication).
